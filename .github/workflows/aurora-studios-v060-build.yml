name: Archived Aurora Studios v0.6.0

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Aurora Studios v0.6.0 hattı arşivlendi; güncel sürüm workflow'unu kullanın."
