name: Archived Aurora Studio Windows v0.8.1

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Windows Studio v0.8.1 hattı arşivlendi; güncel Windows Studio workflow'unu kullanın."
